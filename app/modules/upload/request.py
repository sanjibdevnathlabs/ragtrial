"""
Upload module request models.

Contains Pydantic models for upload API requests.
Currently, upload uses multipart/form-data so no request models needed.
This file exists for consistency and future extensibility.
"""

# No request models currently needed for upload
# Upload uses multipart/form-data (handled by FastAPI's UploadFile)

